# Hark Android fork

This fork targets Android only. Keep Google sign-in through Better Auth, a Node/SQLite backend, and direct FCM delivery. Expo is the local app framework; EAS and Expo Push Service are not required.

Run project commands through `direnv exec "$PWD" ...` after the flake is available. Native Android output is generated by Expo prebuild: put maintained native code in local modules and config plugins, not generated `apps/expo/android` files.

Use an explicit `adb -s SERIAL` and reuse the task's emulator. The user's Pixel 9 is reserved for user-assisted testing after 06:00 America/New_York on 2026-09-12. Do not connect to, install on, or notify the physical phone tonight.

The base `https://sietch.sole-pierce.ts.net` endpoint belongs to T3. Hark uses a separate HTTPS port, initially 8443. Preserve every existing Tailscale Serve mapping and do not enable Funnel.

Keep credentials outside Git and frontend environment variables. Disable upstream deployment/publishing workflows before pushing this fork. Agents own only their assigned files; the coordinator owns dependency installation, lockfile integration, commits, deployment and final checks unless explicitly delegated.

The user authorized continued implementation after the original 2026-09-12 10:00 UTC cutoff. Stop all agent work if remaining weekly Codex usage reaches 90% or lower. The coordinator checks the session's seven-day rate-limit meter. See `docs/android-implementation.md` for protocol and ownership decisions.
